# Only the bulky alert exports go through LFS. Inventories are a few kilobytes
# of JSON and are better kept as ordinary git objects.
data/raw/*_wazuh.json  filter=lfs diff=lfs merge=lfs -text
data/raw/*_aminer.json filter=lfs diff=lfs merge=lfs -text
